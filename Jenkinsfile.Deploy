pipeline {
agent {
    kubernetes {
      	cloud 'kubernetes'
      	defaultContainer 'ubuntu'
      }
    }
    stages {
      stage('Build') {
        steps {
            sh 'sleep 3600'
            }
          }  
}
}
