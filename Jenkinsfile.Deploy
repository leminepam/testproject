pipeline {
    agent {
        kubernetes {
            yaml '''
apiVersion: v1
kind: Pod
spec:
  containers:
  - name: shell
    image: ubuntu
    command:
    - sleep
    args:
    - infinity
'''
            defaultContainer 'shell'
        }
    }
  stages {
    stage('Deploy App') {
      steps {
        kubernetesDeploy(
                    kubeconfigId: 'bdcaff95-dfae-4afd-a2a3-a6656c829ff5',
                    configs: 'hellodocker.yaml'
                )
      }
    }
  }
}
