node {
    checkout scm

    docker.withRegistry("https://registry.hub.docker.com", "leminepam") {

        def customImage = docker.build("leminepam/dockerhub:second")
        customImage.push()
    }
}
